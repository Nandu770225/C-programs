#include<stdio.h>
int main()
{ 
    int x=9,y=7,i;
    if(x==0)
    {
        y++;
        printf("%d",y);
    }
    else if(x>0)
    {
        y=-i;
        printf("%d",y);
    }
    else{
        y+=2;
        printf("%d",y);
    }
    return 0;
}
